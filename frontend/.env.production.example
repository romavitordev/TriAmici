# ───────────────────────────────────────────────────────────
# Frontend — Produção
# Copie para frontend/.env.production.local
# ───────────────────────────────────────────────────────────
# URL pública da API. Com o Nginx fazendo proxy de /api no mesmo domínio,
# basta usar o domínio raiz (as chamadas vão para https://triamici.com.br/api).
NEXT_PUBLIC_API_URL=https://triamici.com.br
