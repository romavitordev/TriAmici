node_modules

# Build output
backend/dist
frontend/.next

# Ambiente (segredos nunca versionados — exceto os *.example)
.env
.env.*
!.env.example
!.env.production.example

# Logs / PM2
*.log
.pm2

# Helper de DB local efêmero (dev)
backend/_devdb.mjs

# config local do Claude Code
.claude/
